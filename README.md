# Alarm-Clock-with-Python
This project is a simple Alarm Clock application developed in Python. It allows users to set an alarm by entering a specific time in HH:MM:SS AM/PM format. The program continuously monitors the system time and, when the set time is reached, plays a selected audio file and displays a wake-up notification, helping users manage their time effectively.
